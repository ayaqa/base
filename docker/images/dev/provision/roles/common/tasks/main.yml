---
- name: "Include packages tasks: install-{{ ansible_distribution | lower }}.yml"
  include_tasks:
    file: "install-{{ ansible_distribution | lower }}.yml"
    apply:
      tags: 
        - provision
        - install
  tags: 
    - provision
    - install